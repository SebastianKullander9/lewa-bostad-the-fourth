export type Background = "default" | "alt" | "brand";

export type KeyFigure = {
    value: string;
    suffix: string;
    label: string;
};

export type textBlock = {
    title: string;
    text: string;
};
